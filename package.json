{
  "name": "threetwo-docs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "docmd dev",
    "build": "docmd build",
    "start": "docmd build && docmd serve",
    "deploy": "docmd deploy"
  },
  "dependencies": {
    "@docmd/core": "^0.8.10"
  },
  "engines": {
    "node": ">=18.0"
  },
  "type": "module",
  "devDependencies": {
    "octokit": "^5.0.5"
  }
}
